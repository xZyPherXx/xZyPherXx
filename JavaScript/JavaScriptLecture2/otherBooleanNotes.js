
const x = 0;
if (x) console.log('x contains a value');
else console.log('x contains *NO* value'); //* use if (!x) { code... }

const name = 'ZyPherX';
if (name === 'ZyPherX') console.log('The values ate the same');
else console.log('The values NOT are the same');